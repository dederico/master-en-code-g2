/*


El algoritmo de busqueda recibe como argumento
ENTRADAS:
1) Lista con elementos 
2) Elementos o item a buscar 

SALIDAS: 
Este algoritmo devuelve si encuentra el elemento, la posicion (numero)
si no encuentra el elemento, devuelve -1 
*/

const linearSearch = (list, itemToFind) => {
  /* 
    Encontrar el tamano de la lista
    definir el indice desde donde vamos a iterar

    size: --> desde donde iteramos
    index: --> hasta donde iteramos 

    if index < size

    else

    return -1

        Iteramos sobre la lista a partir de su tamano

    */
};
